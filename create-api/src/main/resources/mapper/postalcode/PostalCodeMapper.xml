<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tw.com.insurance.api.postalcode.persistence.PostalCodeMapper">
  <select id="findZipCode3" resultType="map">
    SELECT code_before, code_after, code_description
                  FROM main.code_definition
                 WHERE code_group = 'postal-code'
                   AND code_field = 'zip_code3'
                   AND code_before = #{zipCode3}
  </select>
</mapper>
