# 目前所有的資安弱點與安全開發基準

> 本文件提供安全檢查候選項目，不代表本 repository 的即時掃描結果。實際判定須由 `skills/review-generated-code/SKILL.md` 路由到適用的 dependency、secret、container、SAST 與 runtime 檢查，並分別驗證 `<backend-module>`、`<batch-module>` 與 `<frontend-module>`。

更新日期：2026-08-02

## 文件定位

「所有弱點」無法形成永久封閉的清單；CVE、攻擊手法與框架版本會持續新增。本文件採「弱點分類全集 + 本專案檢查結果 + 每次交付閘門」，而不是宣稱列出網路上每一個 CVE。每次開發至少依最新官方版本重查 OWASP、CWE、NIST 與使用框架的安全公告。

## 官方基準

- [OWASP Top 10:2025](https://owasp.org/Top10/2025/)：Web 應用程式主要風險。
- [OWASP API Security Top 10:2023](https://owasp.org/API-Security/editions/2023/en/0x11-t10/)：API 特有授權、資源與業務流程風險。
- [MITRE CWE Top 25:2025](https://cwe.mitre.org/top25/archive/2025/2025_cwe_top25.html)：依真實 CVE 統計的高危程式弱點。
- [OWASP ASVS 5.0](https://owasp.org/www-project-application-security-verification-standard/)：驗證安全控制的可測試需求基準。
- [NIST SP 800-218 SSDF 1.1](https://csrc.nist.gov/pubs/sp/800/218/final)：安全軟體生命週期。
- [Vue Security](https://vuejs.org/guide/best-practices/security.html)、[Spring Security Headers](https://docs.spring.io/spring-security/reference/servlet/exploits/headers.html)、[Kubernetes Security Checklist](https://kubernetes.io/docs/concepts/security/security-checklist/)：本專案技術棧官方要求。

## 必查弱點分類

| 分類 | 常見弱點 | 本專案強制控制 |
|---|---|---|
| 存取控制 | IDOR/BOLA、水平或垂直越權、功能與欄位層授權缺失 | API 驗證身分、角色、功能碼、物件及欄位；前端隱藏按鈕不算授權 |
| 認證與工作階段 | 弱密碼、暴力破解、憑證固定、明文或長效儲存 | TLS、BCrypt、登入限流、通用錯誤；瀏覽器不得持久化 Basic/JWT |
| 注入 | SQL/NoSQL/LDAP/OS command/template/header/CRLF | MyBatis 僅 `#{}`；禁 `${}`、拼 SQL、shell 接收輸入、動態執行碼 |
| XSS | Stored、Reflected、DOM XSS、危險 URL/CSS/HTML | Vue 插值編碼；禁 `v-html`、`innerHTML`、`eval`；必要時 sanitizer + CSP |
| CSRF/CORS | Cookie 偽造請求、寬鬆 Origin、credential wildcard | Cookie 認證必開 CSRF；明確 Authorization header 才可停用；Origin allowlist |
| 密碼與機密 | 硬編碼 secret、弱雜湊、金鑰無輪替 | Secret Manager/K8s Secret/環境變數；正式無預設密碼；資料依分級加密 |
| 輸入與輸出 | 未驗證、Mass Assignment、過度回傳、不安全反序列化 | Request DTO allowlist + Bean Validation；最小回應；不直接綁持久化 Entity |
| 檔案與路徑 | Path traversal、Zip Slip、任意上傳/下載 | 固定根目錄、規範化、檔型/內容/大小檢查、重新命名、禁執行、掃毒 |
| SSRF/外部 API | 任意 URL、內網探測、轉址、無 timeout | scheme/host/port allowlist、禁私網、限制轉址與大小、timeout、驗證回應 |
| 業務流程 | 重放、競態、重複送審、Maker 自覆核、狀態跳轉 | 唯一鎖、版本、狀態機、Maker-Checker、冪等鍵、交易與稽核 |
| 資源耗盡 | 無分頁/大小限制、ReDoS、批次與登入濫用 | 限流、20 筆預設分頁、body/file/欄位上限、timeout、資源 quota |
| 錯誤處理 | Stack trace/SQL/個資洩漏、fail-open | `ResponseBodyDto`、通用對外訊息、correlation ID、fail-closed |
| Logging/稽核 | 密碼/token/個資、log injection、可竄改、無告警 | 遮罩、結構化、清除 CR/LF、append-only、時鐘同步與異常告警 |
| 供應鏈 | 漏洞元件、typosquatting、未鎖版、惡意 CI、映像漂移 | lockfile、digest、SCA/SBOM、可信 registry、最小 CI 權限、簽章 |
| 部署與雲端 | Debug/Actuator、root、可寫 FS、過度 RBAC、Secret 入映像 | 關 debug、health 最小化、非 root、read-only、drop caps、NetworkPolicy/RBAC |
| 瀏覽器與傳輸 | Clickjacking、MIME sniff、個資快取、混合內容 | CSP、frame deny、nosniff、no-store、Referrer/Permissions Policy、TLS/HSTS |
| 資料與隱私 | 過度蒐集、未遮罩、越權匯出、備份外洩 | 最小化、分級、匯出授權、加密備份、保存/刪除政策、存取稽核 |

## OWASP Top 10:2025 對照

1. A01 Broken Access Control：後端做角色、功能碼、物件與欄位層授權。
2. A02 Security Misconfiguration：安全預設、關 debug、最小暴露與安全標頭。
3. A03 Software Supply Chain Failures：SCA、鎖版、SBOM、可信映像與修補 SLA。
4. A04 Cryptographic Failures：TLS、密碼雜湊、資料分級及金鑰生命週期。
5. A05 Injection：參數化查詢、輸入 allowlist、情境輸出編碼。
6. A06 Insecure Design：威脅建模、濫用案例、狀態機與 Maker-Checker。
7. A07 Authentication Failures：帳號安全、限流、失敗訊息與憑證保存。
8. A08 Software or Data Integrity Failures：簽章、來源驗證、反序列化及 CI/CD 完整性。
9. A09 Security Logging and Alerting Failures：可追蹤、不可竄改、可告警且不洩密。
10. A10 Mishandling of Exceptional Conditions：邊界錯誤、資源釋放、回滾與 fail-closed。

## 本次專案檢視（2026-07-19）

| 項目 | 結果 | 本次處理 |
|---|---|---|
| SQL Injection | 通過靜態檢查 | Mapper 使用 `#{}`；未發現 `${}` 動態 SQL |
| Vue XSS 危險 API | 通過靜態檢查 | 未發現 `v-html`、`innerHTML`、`eval`；規則加入 skill |
| API 授權/欄位綁定 | 已改善，仍需負向測試 | Spring 角色 + `ScreenAuthorizationFilter`；地址/主附約維護改 Request DTO allowlist，稽核欄位不可由請求覆寫 |
| CORS/CSRF | 可接受但有前提 | CORS 無 credentials；Authorization header 明確送出。改 Cookie 時必須啟用 CSRF |
| 憑證保存 | 已改善 | 移除 `sessionStorage`，改分頁記憶體；重新整理即登出 |
| 固定本機密碼 | 已改善 | local 設定不再含 DB 或登入預設密碼 |
| 安全標頭 | 已改善 | Nginx 已有 CSP/HSTS/no-store；API 補 CSP、frame、Referrer、Permissions Policy |
| 資源耗盡 | 已改善 | Nginx 限流/1MB；Spring 補 form/multipart 1MB；容器有 CPU/記憶體限制 |
| 錯誤洩漏 | 通過 | 對外不回 stack trace/SQL；非預期錯誤統一訊息 |
| Log 個資 | 基本通過 | 關閉 MyBatis 原始參數 log 並遮罩；正式平台仍須設定偵測告警 |
| 容器 | 通過設定檢查 | digest、非 root、read-only、drop ALL、no-new-privileges |
| 前端正式相依套件 | 通過 | `npm audit --omit=dev`：0 個已知弱點 |
| 後端相依套件 | 本機掃描未完成 | 已加入 OWASP Dependency-Check 12.2.2、CVSS 7 阻擋門檻與 CI 工作；首次 NVD 同步至 30% 後遭 HTTP 429 限流，CI 必須提供 `NVD_API_KEY` 後重跑，不得解讀為零弱點 |

## 尚待環境與治理完成

- 正式壽險系統建議把 Basic Auth 改為企業 IdP 的 OIDC/OAuth2、短效 token、MFA 與集中撤銷；完成前強制 TLS。
- Ingress/API Gateway 應提供登入及敏感業務流程的使用者級限流；目前 Nginx 為 IP 級限流。
- K8s 上線前補 NetworkPolicy、Pod Security、最小 RBAC、Secret 加密、映像簽章與集中告警。
- CI 固定執行 SAST、SCA、secret/IaC scan、SBOM、授權負向測試；掃描失敗應標為 blocked，不能當通過。
- 依 ASVS 5.0 建立可追蹤測試矩陣並安排授權的 DAST/滲透測試；自動掃描不能取代人工業務邏輯測試。

## 每次產碼完成條件

1. 先做威脅與資料分級，列出身分、物件、狀態、信任邊界及濫用方式。
2. 後端授權與驗證完成後才做 UI；不得只靠 route guard、disabled 或隱藏按鈕。
3. CRUD 驗證重複 KEY、合法狀態、版本、Maker-Checker 與稽核軌跡。
4. 新 API 預設 deny、DTO allowlist、參數化查詢、限制筆數/大小/時間、輸出最少欄位。
5. 新依賴先驗證來源、版本、授權與漏洞；更新 lockfile/SBOM。
6. 測試包含未登入、錯角色/畫面/物件、越界值、重放、競態、注入字串與例外。
7. 更新本文件、前後端 `skill.md` 與 `readme.md`，記錄已修正及殘餘風險。
