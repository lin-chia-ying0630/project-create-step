# POS 專案欄位命名盤點

> 參考快照：本文件盤點既有 `pos-api` 與 `pos-web`，不是本 repository 新專案的權威契約。原先引用的 `欄位命名規則ＤＤ定義.md` 未收錄，來源狀態為 `Unknown`；實作前須取得正式 DD 或以新 OpenAPI／schema 核准結果取代。

> 2026-07-19 實作狀態：`V37`～`V40` 已建立 canonical 欄位、保全欄位、核心保單表與治理表遷移；Java 實體、MyBatis、API、前端型別、`CHT-code` 與舊覆核 alias 已同步。資料庫實跑仍須在 Docker／MySQL 可用時驗證。

> 2026-07-24 擴檔狀態：V49 已擴充核心欄位容量；V50 已補齊目前三個核心檔應保存的契約狀態／日期／期間／商品／要保書／客戶／業務員、獨立聯絡方式及保障項目商品與期間資料。要保人、被保險人、受益人、收費、保單價值、借款、理賠、核保告知與投資帳戶屬一對多或獨立生命週期，後續應建立獨立 canonical table，不塞入 `policy_contract`。舊地址混合欄位暫留相容歷史快照，只有 postalCode/addressText 可明確回填。

盤點範圍：Flyway SQL、MyBatis Mapper、Java entity／DTO／service／controller、Vue／TypeScript API／store／schema／component／test、稽核 JSON 快照與 `CHT-code`。

## 1. 結論

| 等級 | 數量 | 處理原則 |
| --- | ---: | --- |
| P0 語意錯誤或識別錯誤 | 4 組 | 新舊欄位並存遷移，不可直接 rename |
| P1 語意不完整或資料混放 | 8 組 | 先確認企業 DD 與資料轉換，再提供相容 API |
| P2 一致性命名 | 8 組 | 可分批改程式，但須保留 API 相容期 |
| 保留 | 15 組以上 | 已符合 DD，不為一致而改名 |

目前命中待檢查名稱約 1,440 處；這是程式文字命中數，不等於資料庫欄位數。主要集中於 baseline migration、`PolicyChangeDao.xml`、保全服務與前端 API/store。

## 2. P0：優先處理

| 現有名稱 | 建議名稱 | 問題 | 主要影響 |
| --- | --- | --- | --- |
| `main_policy_ride`／`MainPolicyRide` | `policy_coverage`／`PolicyCoverage` | `ride` 不是附約，且現況同時保存主約與附約 | DB 表、Entity、DAO、Service、DTO、route、Vue、測試、歷史快照 |
| `ride_type`／`rideType` | `coverage_item_type`／`coverageItemType` | 必須能同時表達 BASE 與 RIDER | 同上及 `CodeTable`、`CHT-code` |
| `ride_order`／`rideOrder` | `coverage_item_seq`／`coverageItemSeq` | `order` 易被理解為排序或訂單 | PK、查詢參數、覆核 uniqueKey、API |
| `username` 作授權主鍵 | `user_id`／`userId` | 授權應掛不可變使用者 ID，不應掛登入顯示名稱 | `users`、`authorities`、`user_screen_authorization`、Security、JWT／session、前端 auth |

## 3. P1：需先確認業務定義

| 現有名稱 | 候選 canonical name | 必須先確認的問題 |
| --- | --- | --- |
| `main_policy_years`、`policy_years` | `coverage_term_years` 或 `premium_payment_term_years` | 是保險期間還是繳費期間？主約與附約是否相同？ |
| `premium` | `premium_amount`、`initial_premium_amount`、`renewal_premium_amount` 或其他 | 是年繳保費、當期保費、標準保費還是實收保費？ |
| `main_product_code` | `base_plan_product_code` | 是否確實代表主約商品代碼？若只是商品代碼應用 `product_code` |
| `address_type` | `address_type_code` | 目前同時放地址、電話、email 類型，是否應拆成 contact method？ |
| `zip_code3`＋`zip_code2` | `postal_code` | 是否須保留舊 3+2 輸入相容？轉為六碼時如何補值？ |
| `full_width_address`＋`half_width_address` | `address_text`、`email_address`、`telephone_no`、`mobile_no` | 目前欄位被用來承載不同種類資料，必須依 address type 搬移 |
| `policy_seq` | 保留或改成明確版本／契約序號 | 所有畫面已移除序號，但 DB 仍是複合 PK；必須先確認真正業務意義 |
| `unique_key` | 保留，但需結構化規格 | VARCHAR(255) 與 review_key 512 長度不一致；需定義 escaping、順序及版本 |

## 4. P2：一致性調整

| 現有名稱 | 建議名稱 | 說明 |
| --- | --- | --- |
| `change_item`／`changeItem` | `change_item_code`／`changeItemCode` | 值是代碼，名稱應帶 Code |
| `change_field`／`changeField` | `changed_field_name`／`changedFieldName` | 表達「被變更欄位名稱」 |
| `change_key`／`changeKey` | `changed_record_key`／`changedRecordKey` | 表達被變更紀錄完整業務鍵 |
| `change_file`／`changeFile` | `changed_record_type`／`changedRecordType` | 現況代表 table/entity，不是檔案 |
| `operation` | `operation_type`／`operationType` | CREATE／UPDATE／DELETE 是作業類型 |
| `action`（稽核表） | `audit_action`／`auditAction` | 避免與 UI action、業務 operation 混淆 |
| `source_type` | `source_system_code` 或 `source_domain_code` | 依實際值判斷是系統還是業務領域 |
| `source_record_type` | `source_record_type_code` | 若值為固定列舉則明示為 code |

## 5. 可保留名稱

下列名稱語意清楚且符合 DD，沒有業務理由時不應修改：

- `policy_no`、`change_case_no`、`acceptance_status`、`review_status`
- `insured_amount`、`product_code`、`function_code`
- `content_before`、`content_after`、`review_key`、`review_remark`
- `created_by`、`created_at`、`updated_by`、`updated_at`、`reviewed_by`、`reviewed_at`
- `operator_id`、`request_id`、`trace_id`、`occurred_at`
- `code_group`、`code_field`、`code_before`、`code_after`、`code_description`（代碼設定領域專用）

## 6. 分層影響清單

### 資料庫與 Flyway

- `V1__baseline.sql`：所有保單主檔、地址、附約及保全核心舊名稱。
- `V6__remove_duplicated_master_policy_fields.sql`、`V7__backfill_main_ride_change_snapshots.sql`：舊名稱資料搬移與 JSON 快照。
- `V3__add_cht_json_field_names.sql`、`V31__add_change_review_detail_labels.sql`、`V35__complete_cht_field_names.sql`：中文欄位 metadata。
- `V36__add_user_screen_authorization.sql`：`username` 應改由 `user_id` 授權。
- 既有 migration 不回寫；須新增版本化 migration，並確保空庫與既有庫都能升級。
- `V37`～`V40` 依序改為 `policy_contract`、`policy_contact`、`policy_coverage`、`policy_change_record_snapshot`、`code_definition`、`user_account`、`user_role_assignment`，並統一 `user_id`。
- 保全流程表固定為 `policy_change_item`、`policy_change_field`、`policy_change_record_snapshot`；不得將欄位 `change_item_code`、`changed_field_name`、`changed_record_type` 誤作表名後綴。

### 後端

- Entity／DTO：`MainPolicyRide`、`RideAmountChangeRequest`、`RiderAmountChangeListRequest.rides`、`PolicyDetailDto.rideList`、`MainPolicyAddress`、`AddressChangeRequest`、`MainPolicyMaster`。
- Enum：`CodeTable`、`PolicyRideKey`、`PolicyChangeFieldName`。
- Persistence：`PolicyChangeDao.java`、`PolicyChangeDao.xml`。
- Service：地址變更、保額變更、保單查詢、保單維護、保全案件與使用者授權服務。
- 相容策略：新 DTO 使用 canonical name；舊 JSON 可用 `@JsonAlias` 暫時接收，回傳應公告切換版本。

### 前端

- API／schema／store：`src/api/posChange.ts`、`src/schemas/changeCaseSchemas.ts`、`addressChangeStore.ts`、`amountChangeStore.ts`、`policyStore.ts`。
- View／component：保單主檔、地址、主附約、保全新增、覆核預覽與動態欄位元件。
- 路由 `/policy/rides` 應調整為 `/policy/riders`，並保留 redirect 相容期。
- 中文標籤仍由 API metadata／`CHT-code` 取得，不因欄位 rename 回到頁面寫死。

### 歷史資料與稽核

- `content_before`、`content_after` 內含舊 JSON key，不能只改 Java 欄位。
- 覆核中的 `unique_key`／`review_key` 可能包含 `ride_order` 或舊表名；須先建立 key 版本或轉換器。
- `CHT-code` 同時保留舊 key alias，直到待覆核資料全部完成或取消。
- 稽核軌跡應保存「當時欄位名」與 canonical name 對照，避免歷史畫面無法解讀。

## 7. 建議遷移順序

1. 核定 `policy_seq`、`premium`、`policy_years`、地址聯絡資料的企業定義。
2. 新增 canonical 欄位／表與雙讀，不修改既有 migration。
3. 回填資料並對帳筆數、金額、PK、覆核狀態與 JSON 快照。
4. 後端改為寫新欄位，舊 API 以 alias／adapter 相容。
5. 前端 API type、schema、store、動態欄位與 route 切換。
6. 完成 pending review 後移除舊 key；再停止舊欄位雙寫。
7. 更新 `CHT-code`、OpenAPI、測試資料、README 與 skill。

## 8. 驗收條件

- 空資料庫及既有資料庫 Flyway 均能成功升級。
- 同一筆保單在遷移前後的保額、保費、地址、附約數量完全一致。
- 舊版 API 請求於相容期間仍可受理，新版回傳只使用 canonical name。
- 所有新增／修改／刪除仍檢查相同完整業務鍵是否有非 `S` 覆核資料。
- 歷史與新建立覆核資料皆能顯示中文欄位名稱及異動前後值。
- `user_id` 授權不可因顯示名稱或登入名稱改變而失效。

## 9. 現行業務邏輯檢視

| 優先級 | 現況 | 風險 | 建議／狀態 |
| --- | --- | --- | --- |
| P0 | 保單、地址、保障項目、代碼在送覆核時已直接更新正式表 | 未核准值立即可見；取消只改狀態，沒有回復原值 | 已改為 `STAGED`；`S` 由 `ChangeReviewApplier` 套用，`C` 不改正式表；LEGACY 案件保留相容流程 |
| P0 | 新增、修改、刪除的同 Key 防重原本不完整 | 可建立多筆互相覆蓋的待覆核資料 | 已加入 `change_review_pending_lock` 複合主鍵鎖；S/C 或 Admin 直接完成後釋放 |
| P0 | reviewKey 使用 before JSON 組合 | 重送相同內容可能撞唯一鍵，且可能超過 512 字 | 已改為 UUID；業務防重仍用 functionCode＋uniqueKey |
| P1 | 正式表缺少 `record_version` | 只能用快照值局部比對，無法一致防止 lost update | 已於契約、聯絡、保障項目及代碼正式表加入樂觀鎖版本 |
| P1 | `POLICY_MASTER`、`POLICY_ADDRESS`、`POLICY_RIDE` 等 source type 仍是舊名稱 | 新舊名稱並存，歷史解析容易分叉 | 新資料已改 canonical code；解析層保留 legacy alias |
| P1 | 刪除先將 `active_flag=N` 再送覆核 | 未核准刪除會立刻從查詢消失 | 已改為 staging，核准後才邏輯刪除 |
| P1 | 覆核 `S` 只同步 review status，未統一套用 contentAfter | 覆核中心與正式資料職責顛倒 | 已建立 per-source applier，在同一交易套用及寫 audit |
| P1 | 角色存在但沒有畫面功能仍可直接呼叫 API | 前端隱藏無法形成安全邊界 | 已加入 `ScreenAuthorizationFilter`，後端以 userId＋functionCode 再授權 |
| P1 | Admin 直接完成僅涵蓋使用者授權 | 其他維護仍不必要地進覆核 | 已以 `ReviewExecutionPolicy` 統一直接完成，稽核為 `S / DIRECT_APPLY` |
| P1 | 主附約缺少依附、期間、幣別與金額規則 | 可建立無主約附約或幣別不一致資料 | 已集中至 `InsuranceBusinessValidator`；商品費率規則仍應由企業商品規則服務擴充 |
| P2 | `resolveRideOrder` 等方法仍保留舊領域名稱 | 可讀性下降但不影響資料正確性 | 兼容期後改為 `resolveCoverageItemSeq` |
| P2 | 前端 ESLint 原有 68 個排版 warning | 不影響功能，但降低一致性 | 已執行 ESLint fix，現為 0 warning／0 error |
| P2 | 覆核查詢以 LIKE 掃描前後 JSON | 效能差且可能擴大個資搜尋範圍 | 已新增結構化 `key1` 與索引，停止以 JSON 作一般搜尋 |
| P2 | 稽核表僅靠程式約定 append-only | DB 權限錯誤時可能被覆寫 | 已建立禁止 UPDATE／DELETE trigger，直接完成事件保存 request／trace ID |

### 建議覆核交易模型

1. Maker 輸入資料，後端檢查權限、欄位規則、正式資料版本及同 Key 非完成案件。
2. 只新增 `change_review(P)` 與 `change_review_audit(SUBMIT)`，不改正式表。
3. Reviewer 鎖定 review，重新驗證建檔人不同、狀態為 P、正式資料版本未變。
4. 核准：依 `source_record_type` 呼叫 applier 套用 `content_after`，成功後更新 review=S 並寫 APPROVE audit。
5. 取消：不修改正式表，更新 review=C 並寫 REJECT audit；說明必填。
6. 所有步驟在單一資料庫交易完成，任一步失敗全部 rollback。
