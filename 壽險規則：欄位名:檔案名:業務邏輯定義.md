# 欄位命名規則 DD 定義

> 本文件提供壽險欄位命名候選規則，不是所有新保險專案的已核准需求。使用前先依 `skills/start-insurance-project/SKILL.md` 建立領域與跨層契約；與 `AGENTS.md`、OpenAPI 或實際 schema 衝突時，必須建立 gap 並取得業務核准。

新增資料表、DTO、API、JSON、畫面欄位或繁中 metadata 前，先依 `skills/impact-analysis/SKILL.md` 盤點實際影響，再以本文件作為候選命名參考；目前 schema、OpenAPI 與程式仍是現況證據。

> 適用範圍：台灣個人壽險、健康險、傷害險、年金險的保單與保全作業。公司正式企業 DD 若與本文件不同，以企業 DD 為準，並須先更新本文件後再改程式。

## 1. 命名規則

| 層級 | 規則 | 範例 |
| --- | --- | --- |
| MySQL | snake_case，完整英文單字優先 | `policy_no`, `applicant_customer_code` |
| Java／TypeScript／JSON | camelCase | `policyNo`, `applicantCustomerCode` |
| Java 類別／型別 | PascalCase | `PolicyContract`, `PolicyRider` |
| 固定代碼 | 大寫英文或核准代碼 | `P`, `S`, `C`, `MPS00001` |
| 日期 | `_date`／`Date`，只有年月日 | `effective_date`, `effectiveDate` |
| 日期時間 | `_at`／`At`，含時間與時區語意 | `created_at`, `createdAt` |
| 金額 | `_amount`／`Amount` | `insured_amount`, `insuredAmount` |
| 代碼 | `_code`／`Code` | `product_code`, `productCode` |
| 號碼 | `_no`／`No` | `policy_no`, `policyNo` |
| 狀態 | `_status`／`Status` | `policy_status`, `policyStatus` |
| 旗標 | `_flag`／`Flag`，值限定 Y/N | `active_flag`, `activeFlag` |

禁止縮寫不明的 `data1`、`value1`、`type1`、`temp`。`key1` 只用於跨資料來源查詢，不得作為壽險實體欄位。

## 2. 當事人與通路

| 中文名稱 | DB canonical name | API canonical name | 建議型態 | 定義／市場同義詞 |
| --- | --- | --- | --- | --- |
| 客戶代碼 | `customer_code` | `customerCode` | VARCHAR(32) | 公司內部唯一客戶識別碼 |
| 要保人客戶代碼 | `applicant_customer_code` | `applicantCustomerCode` | VARCHAR(32) | 負擔交付保險費義務之要保人；同義：投保人，不建議作 canonical |
| 被保險人客戶代碼 | `insured_customer_code` | `insuredCustomerCode` | VARCHAR(32) | 保險事故所涉及之被保險人 |
| 受益人客戶代碼 | `beneficiary_customer_code` | `beneficiaryCustomerCode` | VARCHAR(32) | 依約享有保險金請求權之人 |
| 要保人姓名 | `applicant_name` | `applicantName` | VARCHAR(100) | 不使用 `policyholderName`，避免與被保險人混淆 |
| 被保險人姓名 | `insured_name` | `insuredName` | VARCHAR(100) | `insured` 作名詞時代表被保險人 |
| 受益人姓名 | `beneficiary_name` | `beneficiaryName` | VARCHAR(100) | 多人時應另建明細表，不用逗號字串 |
| 身分證明文件類型 | `identity_document_type` | `identityDocumentType` | VARCHAR(8) | 身分證、居留證、護照等 |
| 身分證明文件號碼 | `identity_document_no` | `identityDocumentNo` | VARCHAR(20) | 個資欄位，Log 必須遮罩 |
| 出生日期 | `birth_date` | `birthDate` | DATE | 不使用 age 作永久資料 |
| 性別代碼 | `gender_code` | `genderCode` | VARCHAR(2) | 代碼定義由 code table 提供 |
| 保險業務員代碼 | `insurance_agent_code` | `insuranceAgentCode` | VARCHAR(32) | 保險招攬人員識別；畫面可簡稱業務員代碼 |
| 保險業務員姓名 | `insurance_agent_name` | `insuranceAgentName` | VARCHAR(100) | 不與代理人 `insurance_agent` 法律角色混用 |
| 通路代碼 | `distribution_channel_code` | `distributionChannelCode` | VARCHAR(16) | 業務員、銀行保代、網路等通路 |
| 招攬單位代碼 | `soliciting_unit_code` | `solicitingUnitCode` | VARCHAR(32) | 負責招攬之組織單位 |

## 3. 保單契約

| 中文名稱 | DB canonical name | API canonical name | 建議型態 | Key／定義 |
| --- | --- | --- | --- | --- |
| 保單號碼 | `policy_no` | `policyNo` | VARCHAR(20) | Policy Key1；文數字 |
| 保單序號 | `policy_seq` | `policySeq` | INT | 只有企業 DD 明確定義其用途時使用 |
| 保單狀態 | `policy_status` | `policyStatus` | VARCHAR(2) | 有效、停效、終止、滿期等代碼 |
| 契約成立日 | `contract_date` | `contractDate` | DATE | 保險契約成立日期 |
| 保單生效日 | `effective_date` | `effectiveDate` | DATE | 承保責任開始日期 |
| 保單滿期日 | `maturity_date` | `maturityDate` | DATE | 契約約定滿期日期 |
| 繳費年期 | `premium_payment_term_years` | `premiumPaymentTermYears` | INT | 不使用語意模糊的 `policy_years` |
| 保險期間年期 | `coverage_term_years` | `coverageTermYears` | INT | 終身型可搭配期間類型代碼 |
| 保險期間類型 | `coverage_term_type` | `coverageTermType` | VARCHAR(8) | 年期、歲滿、終身等 |
| 保額／保險金額 | `insured_amount` | `insuredAmount` | DECIMAL(18,2) | 契約約定保險金額 |
| 保費 | `premium_amount` | `premiumAmount` | DECIMAL(18,4) | 建議取代語意較寬的 `premium` |
| 繳別代碼 | `payment_frequency_code` | `paymentFrequencyCode` | VARCHAR(4) | 年繳、半年繳、季繳、月繳等 |
| 幣別代碼 | `currency_code` | `currencyCode` | CHAR(3) | 建議 ISO 4217 |
| 商品版本 | `product_version` | `productVersion` | VARCHAR(32) | 與商品代碼共同決定規則版本，不得只保留最新版本 |
| 商品代碼 | `product_code` | `productCode` | VARCHAR(32) | 保險商品識別碼 |
| 商品名稱 | `product_name` | `productName` | VARCHAR(200) | 核准商品名稱 |
| 主約商品代碼 | `base_plan_product_code` | `basePlanProductCode` | VARCHAR(32) | 主契約商品代碼 |

## 4. 主約與附約

| 中文名稱 | DB canonical name | API canonical name | 建議型態 | 現況／處理 |
| --- | --- | --- | --- | --- |
| 契約項目序號 | `coverage_item_seq` | `coverageItemSeq` | INT | 主約與附約共用的明細序號 |
| 契約項目類型 | `coverage_item_type` | `coverageItemType` | VARCHAR(8) | `BASE`／`RIDER` |
| 附約序號 | `rider_seq` | `riderSeq` | INT | 建議取代 `ride_order` |
| 附約類型 | `rider_type` | `riderType` | VARCHAR(8) | 建議取代 `ride_type` |
| 附約商品代碼 | `rider_product_code` | `riderProductCode` | VARCHAR(32) | 若表本身即 rider，可使用 `product_code` |

禁止使用 `ride` 表示附約；英文 `ride` 是乘坐，市場與契約語意應使用 `rider`。

## 5. 地址與聯絡資料

| 中文名稱 | DB canonical name | API canonical name | 建議型態 | 定義 |
| --- | --- | --- | --- | --- |
| 地址類型代碼 | `address_type_code` | `addressTypeCode` | VARCHAR(8) | 戶籍、通訊、收費等 |
| 郵遞區號 | `postal_code` | `postalCode` | VARCHAR(6) | 不拆 `zip_code3`／`zip_code2` 作新模型 |
| 地址 | `address_text` | `addressText` | VARCHAR(300) | 完整地址文字 |
| 電子郵件地址 | `email_address` | `emailAddress` | VARCHAR(254) | 不放入 address text |
| 市內電話號碼 | `telephone_no` | `telephoneNo` | VARCHAR(30) | 保存字串，避免遺失前導零 |
| 行動電話號碼 | `mobile_no` | `mobileNo` | VARCHAR(30) | 保存字串 |

禁止用 `full_width_address`、`half_width_address` 同時承載地址、email、電話及手機；應依資料種類拆欄位。

## 6. 保全作業與覆核

| 中文名稱 | DB canonical name | API canonical name | 建議型態 | 定義 |
| --- | --- | --- | --- | --- |
| 保全案號 | `change_case_no` | `changeCaseNo` | VARCHAR(20) | 保全申請案件唯一號碼 |
| 保全項目代碼 | `change_item_code` | `changeItemCode` | VARCHAR(8) | 建議取代 `change_item` |
| 受理狀態 | `acceptance_status` | `acceptanceStatus` | VARCHAR(1) | 案件受理流程狀態 |
| 覆核狀態 | `review_status` | `reviewStatus` | VARCHAR(1) | `P` 受理中、`S` 完成、`C` 取消 |
| 作業類型 | `operation_type` | `operationType` | VARCHAR(16) | CREATE、UPDATE、DELETE |
| 完整業務鍵 | `unique_key` | `uniqueKey` | VARCHAR(512) | 來源資料全部主要 Key 的固定順序組合 |
| 覆核識別鍵 | `review_key` | `reviewKey` | VARCHAR(512) | 一次覆核資料的唯一識別，不等於業務 Key |
| 異動前內容 | `content_before` | `contentBefore` | JSON／LONGTEXT | 結構化快照 |
| 異動後內容 | `content_after` | `contentAfter` | JSON／LONGTEXT | 結構化快照 |
| 建立人員 | `created_by` | `createdBy` | VARCHAR(128) | 使用者 ID |
| 建立時間 | `created_at` | `createdAt` | DATETIME | 系統時間 |
| 異動人員 | `updated_by` | `updatedBy` | VARCHAR(128) | 使用者 ID |
| 異動時間 | `updated_at` | `updatedAt` | DATETIME | 系統時間 |
| 覆核人員 | `reviewed_by` | `reviewedBy` | VARCHAR(128) | 使用者 ID |
| 覆核時間 | `reviewed_at` | `reviewedAt` | DATETIME | 系統時間 |

## 7. Key1 對照

| 資料來源 | Key1 實際欄位 | 完整 uniqueKey |
| --- | --- | --- |
| 保單主檔 | `policy_no` | `policy_no\|policy_seq` |
| 保單地址 | `policy_no` | `policy_no\|policy_seq\|address_type_code` |
| 保單附約 | `policy_no` | `policy_no\|policy_seq\|rider_seq` |
| 代碼對照表 | `code_group` | `code_group\|code_field\|code_before` |
| 使用者角色／畫面 | `user_id` | `user_id` 或 `SCREEN\|user_id` |

## 8. 現有專案不妥與遷移清單

| 現有名稱 | 問題 | 目標名稱 | 風險 |
| --- | --- | --- | --- |
| `main_policy_ride` | ride 語意錯誤 | `policy_rider` | 高：表、MyBatis、API、JSON、歷史快照 |
| `ride_type`／`ride_order` | ride 語意錯誤、order 易誤解 | `rider_type`／`rider_seq` | 高 |
| `policy_years` | 無法判斷繳費年期或保險期間 | `coverage_term_years` 或 `premium_payment_term_years` | 高，須企業 DD |
| `premium` | 未說明何種保費 | `premium_amount` 或更精確欄位 | 中，須企業 DD |
| `change_item` | 未明示為代碼 | `change_item_code` | 中 |
| `address_type` | 未明示為代碼 | `address_type_code` | 中 |
| `zip_code3`／`zip_code2` | 舊郵遞區號拆分模型 | `postal_code` | 高，需資料轉換 |
| `full_width_address`／`half_width_address` | 格式名稱取代業務意義，且混放聯絡資料 | `address_text`、`email_address`、`telephone_no`、`mobile_no` | 高 |
| 缺少客戶代碼 | 無法可靠依客戶查詢 | `customer_code` 與角色別客戶代碼 | 高，需資料來源 |
| 缺少業務員代碼 | 無法可靠依業務員查詢 | `insurance_agent_code` | 高，需資料來源 |

上述高風險項目不得直接 rename。必須建立 Flyway migration、資料回填、相容 API、MyBatis、測試、`CHT-code` 與歷史快照遷移計畫。

## 9. 投保與核保

| 中文名稱 | DB canonical name | API canonical name | 建議型態 | 定義 |
| --- | --- | --- | --- | --- |
| 要保日期 | `application_date` | `applicationDate` | DATE | 要保人提出投保申請日期 |
| 要保書號碼 | `application_no` | `applicationNo` | VARCHAR(32) | 要保書唯一號碼，不等於保單號碼 |
| 要保人與被保險人關係代碼 | `applicant_insured_relationship_code` | `applicantInsuredRelationshipCode` | VARCHAR(8) | 關係由 code table 管理 |
| 國籍代碼 | `nationality_code` | `nationalityCode` | CHAR(3) | 建議 ISO 3166-1 alpha-3 |
| 婚姻狀況代碼 | `marital_status_code` | `maritalStatusCode` | VARCHAR(4) | 核保基本資料代碼 |
| 職業代碼 | `occupation_code` | `occupationCode` | VARCHAR(16) | 公司核保職業分類 |
| 職業類別 | `occupation_class` | `occupationClass` | VARCHAR(4) | 職業風險級別，不與職稱混用 |
| 任職公司名稱 | `employer_name` | `employerName` | VARCHAR(200) | 被保險人服務單位 |
| 職稱 | `job_title` | `jobTitle` | VARCHAR(100) | 實際工作職稱 |
| 年收入金額 | `annual_income_amount` | `annualIncomeAmount` | DECIMAL(18,2) | 財務核保資料 |
| 身高公分 | `height_cm` | `heightCm` | DECIMAL(5,2) | 被保險人告知資料 |
| 體重公斤 | `weight_kg` | `weightKg` | DECIMAL(5,2) | 被保險人告知資料 |
| 健康告知答案 | `health_disclosure_answer` | `healthDisclosureAnswer` | JSON | 應保存題目版本、題號及答案，不以固定欄位覆蓋歷史題目 |
| 告知事項題號 | `disclosure_question_code` | `disclosureQuestionCode` | VARCHAR(32) | 對應要保書版本之題目代碼 |
| 告知答案代碼 | `disclosure_answer_code` | `disclosureAnswerCode` | VARCHAR(8) | YES、NO 或核准代碼 |
| 疾病名稱 | `disease_name` | `diseaseName` | VARCHAR(200) | 健康告知明細 |
| 就診醫療機構名稱 | `medical_institution_name` | `medicalInstitutionName` | VARCHAR(200) | 健康告知或理賠調查使用 |
| 就診日期 | `medical_visit_date` | `medicalVisitDate` | DATE | 無確切日可另定年月精度欄位 |
| 核保案件號碼 | `underwriting_case_no` | `underwritingCaseNo` | VARCHAR(32) | 核保流程唯一案件號碼 |
| 核保決定代碼 | `underwriting_decision_code` | `underwritingDecisionCode` | VARCHAR(8) | 承保、加費、除外、延期、拒保等 |
| 核保狀態 | `underwriting_status` | `underwritingStatus` | VARCHAR(8) | 核保流程狀態，不等於核保決定 |
| 核保人員 ID | `underwriter_id` | `underwriterId` | VARCHAR(128) | 作成核保決定之人員 ID |
| 核保完成時間 | `underwritten_at` | `underwrittenAt` | DATETIME | 核保完成時間 |
| 次標準體加費率 | `extra_premium_rate` | `extraPremiumRate` | DECIMAL(9,6) | 加費承保條件 |
| 除外責任代碼 | `exclusion_code` | `exclusionCode` | VARCHAR(32) | 特別除外責任代碼 |
| 體檢狀態 | `medical_exam_status` | `medicalExamStatus` | VARCHAR(8) | 體檢安排與完成狀態 |
| 契約撤銷期限日 | `policy_cooling_off_end_date` | `policyCoolingOffEndDate` | DATE | 保單撤銷權期間截止日 |

## 10. 保費、收費與帳務

| 中文名稱 | DB canonical name | API canonical name | 建議型態 | 定義 |
| --- | --- | --- | --- | --- |
| 應繳保費金額 | `premium_due_amount` | `premiumDueAmount` | DECIMAL(18,4) | 本期應繳金額 |
| 實收保費金額 | `premium_paid_amount` | `premiumPaidAmount` | DECIMAL(18,4) | 本期實際收取金額 |
| 首期保費金額 | `initial_premium_amount` | `initialPremiumAmount` | DECIMAL(18,4) | 契約首期保費 |
| 續期保費金額 | `renewal_premium_amount` | `renewalPremiumAmount` | DECIMAL(18,4) | 續期應繳保費 |
| 躉繳保費金額 | `single_premium_amount` | `singlePremiumAmount` | DECIMAL(18,4) | 一次繳清保費 |
| 保費到期日 | `premium_due_date` | `premiumDueDate` | DATE | 本期保費到期日 |
| 保費繳納日 | `premium_paid_date` | `premiumPaidDate` | DATE | 實際入帳日期 |
| 下次繳費日 | `next_premium_due_date` | `nextPremiumDueDate` | DATE | 下一期應繳日期 |
| 繳費方式代碼 | `payment_method_code` | `paymentMethodCode` | VARCHAR(8) | 轉帳、信用卡、臨櫃等 |
| 收費狀態 | `collection_status` | `collectionStatus` | VARCHAR(8) | 應收、已收、退票等 |
| 收據號碼 | `receipt_no` | `receiptNo` | VARCHAR(32) | 保費收據識別碼 |
| 金融機構代碼 | `financial_institution_code` | `financialInstitutionCode` | VARCHAR(8) | 銀行或郵局代碼 |
| 分行代碼 | `bank_branch_code` | `bankBranchCode` | VARCHAR(8) | 金融機構分支代碼 |
| 銀行帳戶號碼 | `bank_account_no` | `bankAccountNo` | VARCHAR(34) | 敏感資料，顯示與 Log 必須遮罩 |
| 銀行帳戶戶名 | `bank_account_name` | `bankAccountName` | VARCHAR(100) | 付款或給付帳戶戶名 |
| 信用卡末四碼 | `credit_card_last_four` | `creditCardLastFour` | CHAR(4) | 不保存完整卡號於一般業務表 |
| 保費自動墊繳旗標 | `automatic_premium_loan_flag` | `automaticPremiumLoanFlag` | CHAR(1) | Y/N，要保書約定選項 |
| 保費墊繳金額 | `premium_loan_amount` | `premiumLoanAmount` | DECIMAL(18,4) | 已墊繳保費本金 |

## 11. 保單價值、紅利、借款與復效

| 中文名稱 | DB canonical name | API canonical name | 建議型態 | 定義 |
| --- | --- | --- | --- | --- |
| 保單價值準備金 | `policy_value_reserve_amount` | `policyValueReserveAmount` | DECIMAL(18,4) | 指定基準日之保單價值準備金 |
| 解約金金額 | `surrender_value_amount` | `surrenderValueAmount` | DECIMAL(18,4) | 指定基準日解約可領金額 |
| 保單借款本金 | `policy_loan_principal_amount` | `policyLoanPrincipalAmount` | DECIMAL(18,4) | 尚未清償借款本金 |
| 保單借款利息 | `policy_loan_interest_amount` | `policyLoanInterestAmount` | DECIMAL(18,4) | 尚未清償借款利息 |
| 保單借款利率 | `policy_loan_interest_rate` | `policyLoanInterestRate` | DECIMAL(9,6) | 年利率 |
| 可借款金額 | `available_policy_loan_amount` | `availablePolicyLoanAmount` | DECIMAL(18,4) | 查詢時點可借上限 |
| 紅利給付方式代碼 | `dividend_payment_method_code` | `dividendPaymentMethodCode` | VARCHAR(8) | 現金、抵繳保費、儲存生息等 |
| 保單紅利金額 | `policy_dividend_amount` | `policyDividendAmount` | DECIMAL(18,4) | 已宣告或應給付紅利 |
| 停效日期 | `lapse_date` | `lapseDate` | DATE | 契約停效日 |
| 復效申請日期 | `reinstatement_application_date` | `reinstatementApplicationDate` | DATE | 要保人申請復效日期 |
| 復效日期 | `reinstatement_date` | `reinstatementDate` | DATE | 契約恢復效力日期 |
| 終止日期 | `termination_date` | `terminationDate` | DATE | 契約終止日期 |
| 終止原因代碼 | `termination_reason_code` | `terminationReasonCode` | VARCHAR(8) | 解約、死亡、滿期等原因 |
| 減額繳清旗標 | `reduced_paid_up_flag` | `reducedPaidUpFlag` | CHAR(1) | Y/N |
| 展期定期旗標 | `extended_term_flag` | `extendedTermFlag` | CHAR(1) | Y/N |

## 12. 受益人與給付分配

| 中文名稱 | DB canonical name | API canonical name | 建議型態 | 定義 |
| --- | --- | --- | --- | --- |
| 受益人序號 | `beneficiary_seq` | `beneficiarySeq` | INT | 同一保單／給付項目之順序 |
| 受益人類型代碼 | `beneficiary_type_code` | `beneficiaryTypeCode` | VARCHAR(8) | 身故、滿期、生存等 |
| 受益人與被保險人關係代碼 | `beneficiary_insured_relationship_code` | `beneficiaryInsuredRelationshipCode` | VARCHAR(8) | 親屬或其他關係代碼 |
| 受益比例 | `beneficiary_share_rate` | `beneficiaryShareRate` | DECIMAL(9,6) | 同一層級合計應符合業務規則 |
| 受益順位 | `beneficiary_priority` | `beneficiaryPriority` | INT | 受領順位 |
| 法定受益人旗標 | `statutory_beneficiary_flag` | `statutoryBeneficiaryFlag` | CHAR(1) | Y/N |
| 給付方式代碼 | `benefit_payment_method_code` | `benefitPaymentMethodCode` | VARCHAR(8) | 一次給付、分期給付等 |
| 分期給付期數 | `installment_payment_count` | `installmentPaymentCount` | INT | 分期定期給付期數 |

## 13. 保全變更

| 中文名稱 | DB canonical name | API canonical name | 建議型態 | 定義 |
| --- | --- | --- | --- | --- |
| 保全申請日期 | `policy_change_application_date` | `policyChangeApplicationDate` | DATE | 提出保全申請日期 |
| 保全生效日期 | `policy_change_effective_date` | `policyChangeEffectiveDate` | DATE | 變更生效日期 |
| 保全完成時間 | `policy_change_completed_at` | `policyChangeCompletedAt` | DATETIME | 案件完成時間 |
| 變更原因代碼 | `change_reason_code` | `changeReasonCode` | VARCHAR(8) | 變更原因代碼 |
| 變更欄位名稱 | `changed_field_name` | `changedFieldName` | VARCHAR(128) | canonical 欄位名稱；建議取代 `change_field` |
| 變更欄位業務鍵 | `changed_record_key` | `changedRecordKey` | VARCHAR(512) | 被變更明細之完整業務鍵；建議取代 `change_key` |
| 變更資料類型 | `changed_record_type` | `changedRecordType` | VARCHAR(64) | 被變更的資料實體；不使用 file 表示資料表 |
| 保全附件類型代碼 | `policy_change_attachment_type_code` | `policyChangeAttachmentTypeCode` | VARCHAR(16) | 申請文件分類 |
| 附件識別碼 | `attachment_id` | `attachmentId` | VARCHAR(64) | 檔案儲存系統識別碼 |
| 附件檔名 | `attachment_file_name` | `attachmentFileName` | VARCHAR(255) | 原始顯示檔名 |

## 14. 理賠與保險金

| 中文名稱 | DB canonical name | API canonical name | 建議型態 | 定義 |
| --- | --- | --- | --- | --- |
| 理賠案號 | `claim_case_no` | `claimCaseNo` | VARCHAR(32) | 理賠案件唯一號碼 |
| 理賠申請日期 | `claim_application_date` | `claimApplicationDate` | DATE | 受益人提出申請日期 |
| 理賠受理日期 | `claim_received_date` | `claimReceivedDate` | DATE | 公司受理日期 |
| 理賠狀態 | `claim_status` | `claimStatus` | VARCHAR(8) | 受理、調查、核定、給付、拒賠等 |
| 理賠類型代碼 | `claim_type_code` | `claimTypeCode` | VARCHAR(16) | 身故、失能、醫療、重大疾病、滿期等 |
| 保險事故日期 | `insured_event_date` | `insuredEventDate` | DATE | 承保事故發生日 |
| 保險事故原因代碼 | `insured_event_cause_code` | `insuredEventCauseCode` | VARCHAR(16) | 疾病、意外等 |
| 身故日期 | `death_date` | `deathDate` | DATE | 被保險人身故日期 |
| 失能等級代碼 | `disability_grade_code` | `disabilityGradeCode` | VARCHAR(8) | 使用現行「失能」，不用「殘廢」 |
| 診斷疾病代碼 | `diagnosis_code` | `diagnosisCode` | VARCHAR(16) | 建議註明採用之 ICD 版本 |
| 住院日期 | `hospital_admission_date` | `hospitalAdmissionDate` | DATE | 入院日期 |
| 出院日期 | `hospital_discharge_date` | `hospitalDischargeDate` | DATE | 出院日期 |
| 住院日數 | `hospitalization_days` | `hospitalizationDays` | INT | 依條款計算規則產生，不取代原始日期 |
| 手術日期 | `surgery_date` | `surgeryDate` | DATE | 手術發生日期 |
| 手術項目代碼 | `surgery_item_code` | `surgeryItemCode` | VARCHAR(32) | 條款或公司手術表代碼 |
| 醫療費用金額 | `medical_expense_amount` | `medicalExpenseAmount` | DECIMAL(18,2) | 申請之醫療費用 |
| 申請保險金金額 | `claimed_benefit_amount` | `claimedBenefitAmount` | DECIMAL(18,2) | 申請金額 |
| 核定保險金金額 | `approved_benefit_amount` | `approvedBenefitAmount` | DECIMAL(18,2) | 理賠核定金額 |
| 實付保險金金額 | `paid_benefit_amount` | `paidBenefitAmount` | DECIMAL(18,2) | 實際給付金額 |
| 理賠核定日期 | `claim_decision_date` | `claimDecisionDate` | DATE | 核賠決定日期 |
| 理賠決定代碼 | `claim_decision_code` | `claimDecisionCode` | VARCHAR(8) | 給付、部分給付、拒賠等 |
| 拒賠原因代碼 | `claim_denial_reason_code` | `claimDenialReasonCode` | VARCHAR(16) | 拒賠理由代碼 |
| 給付日期 | `benefit_paid_date` | `benefitPaidDate` | DATE | 保險金實際給付日期 |
| 理賠人員 ID | `claim_examiner_id` | `claimExaminerId` | VARCHAR(128) | 承辦核賠人員 |

## 15. 投資型保險與年金

| 中文名稱 | DB canonical name | API canonical name | 建議型態 | 定義 |
| --- | --- | --- | --- | --- |
| 投資標的代碼 | `investment_fund_code` | `investmentFundCode` | VARCHAR(32) | 連結投資標的識別碼 |
| 投資配置比例 | `allocation_rate` | `allocationRate` | DECIMAL(9,6) | 同一配置日合計應符合業務規則 |
| 投資單位數 | `fund_units` | `fundUnits` | DECIMAL(24,8) | 持有單位數 |
| 單位淨值 | `unit_price` | `unitPrice` | DECIMAL(24,8) | 指定評價日之單位價值 |
| 投資帳戶價值 | `investment_account_value_amount` | `investmentAccountValueAmount` | DECIMAL(18,4) | 指定評價日之帳戶價值 |
| 評價日期 | `valuation_date` | `valuationDate` | DATE | 投資價值計算基準日 |
| 保費費用金額 | `premium_expense_amount` | `premiumExpenseAmount` | DECIMAL(18,4) | 投資型保單相關費用 |
| 保單管理費金額 | `policy_admin_fee_amount` | `policyAdminFeeAmount` | DECIMAL(18,4) | 保單管理費 |
| 保險成本金額 | `cost_of_insurance_amount` | `costOfInsuranceAmount` | DECIMAL(18,4) | 危險保費／保險成本 |
| 年金開始日期 | `annuity_commencement_date` | `annuityCommencementDate` | DATE | 年金給付開始日 |
| 年金給付頻率代碼 | `annuity_payment_frequency_code` | `annuityPaymentFrequencyCode` | VARCHAR(8) | 月、季、半年、年等 |
| 年金給付金額 | `annuity_payment_amount` | `annuityPaymentAmount` | DECIMAL(18,4) | 每期年金金額 |
| 保證給付期間年數 | `guaranteed_payment_period_years` | `guaranteedPaymentPeriodYears` | INT | 保證年金給付期間 |

## 16. 文件、同意與共通治理欄位

| 中文名稱 | DB canonical name | API canonical name | 建議型態 | 定義 |
| --- | --- | --- | --- | --- |
| 文件類型代碼 | `document_type_code` | `documentTypeCode` | VARCHAR(16) | 要保書、診斷書、收據等 |
| 文件識別碼 | `document_id` | `documentId` | VARCHAR(64) | 文件服務唯一識別碼 |
| 文件版本 | `document_version` | `documentVersion` | VARCHAR(16) | 表單或條款版本 |
| 簽署日期時間 | `signed_at` | `signedAt` | DATETIME | 電子或紙本簽署完成時間 |
| 同意類型代碼 | `consent_type_code` | `consentTypeCode` | VARCHAR(32) | 個資、醫療資料查詢、電子通知等 |
| 同意旗標 | `consent_flag` | `consentFlag` | CHAR(1) | Y/N |
| 同意版本 | `consent_version` | `consentVersion` | VARCHAR(16) | 同意條款版本 |
| 資料來源系統代碼 | `source_system_code` | `sourceSystemCode` | VARCHAR(16) | 資料產生系統 |
| 來源紀錄識別碼 | `source_record_id` | `sourceRecordId` | VARCHAR(128) | 來源系統紀錄 ID；可能非數字 |
| 資料版本號 | `record_version` | `recordVersion` | BIGINT | 樂觀鎖或歷史版本 |
| 覆核工作模式 | `workflow_mode` | `workflowMode` | VARCHAR(16) | `LEGACY`、`STAGED`、`DIRECT` |
| 覆核主要查詢鍵 | `key1` | `key1` | VARCHAR(128) | 僅供跨來源查詢，不取代各實體正式業務欄位 |
| 生效起日 | `valid_from_date` | `validFromDate` | DATE | 業務有效期間開始 |
| 生效迄日 | `valid_to_date` | `validToDate` | DATE | 業務有效期間結束 |
| 刪除旗標 | `deleted_flag` | `deletedFlag` | CHAR(1) | 邏輯刪除 Y/N；非必要勿加入 |

## 17. 使用原則與維護流程

1. 新欄位先找業務概念，再選 canonical name；不得從畫面中文直接逐字翻譯。
2. 一個欄位只承載一種業務意義；地址、電話、email、金額、代碼不得混放。
3. 同一英文名稱在 DB、Java、JSON、TypeScript 僅轉換命名格式，不改語意。
4. 顯示中文名稱由後端欄位 metadata／`CHT-code` 提供；找不到時顯示 API 回傳的 canonical key，不在 Vue 寫死標籤。
5. 本文件沒有的欄位，先補 DD 的中文名稱、canonical name、型態、定義、來源與資料擁有者，再開發。
6. `建議型態` 是跨公司基準，不等於本公司最終長度；正式長度、必填、Key、代碼集與個資等級須經企業資料治理核准。
7. 本專案實際差異與遷移順序請查閱 [`專案欄位命名盤點.md`](專案欄位命名盤點.md)。

## 18. 壽險邏輯資料表命名

資料表使用單數、snake_case，名稱表達業務實體，不加無法辨識的 `main_`、`data_`、`file_`。下表是跨公司邏輯模型基準；實際拆表、必填及關聯仍以企業 DD 為準。

| 業務領域 | canonical table | 中文名稱 | 主要 Key／用途 |
| --- | --- | --- | --- |
| 客戶 | `customer` | 客戶主檔 | `customer_id`；自然人或法人共同主體 |
| 客戶證件 | `customer_identity_document` | 客戶身分證明文件 | `customer_id + identity_document_type` |
| 客戶地址 | `customer_address` | 客戶地址 | `customer_id + address_type_code + address_seq` |
| 客戶聯絡 | `customer_contact` | 客戶聯絡方式 | `customer_id + contact_type_code + contact_seq` |
| 保單 | `policy_contract` | 保單契約 | `policy_no`；契約層級狀態、日期與幣別 |
| 保單關係人 | `policy_party` | 保單關係人 | `policy_no + party_role_code + party_seq`；要保人、被保險人等 |
| 保障項目 | `policy_coverage` | 主約／附約保障項目 | `policy_no + coverage_item_seq`；以 `coverage_item_type` 區分 BASE/RIDER |
| 受益人 | `policy_beneficiary` | 保單受益人 | `policy_no + beneficiary_type_code + beneficiary_seq` |
| 保單地址快照 | `policy_address` | 保單地址 | 保留簽約或指定時點地址，不直接覆蓋客戶地址 |
| 保單聯絡快照 | `policy_contact` | 保單聯絡方式 | email、電話、手機等，不與地址文字混欄 |
| 保費計畫 | `policy_premium_schedule` | 保費應繳計畫 | `policy_no + premium_due_date + installment_seq` |
| 保費收款 | `policy_premium_payment` | 保費收款 | `payment_id`；實收、收據與付款方式 |
| 保單價值 | `policy_value` | 保單價值 | `policy_no + valuation_date` |
| 保單借款 | `policy_loan` | 保單借款 | `policy_loan_no` |
| 紅利 | `policy_dividend` | 保單紅利 | `policy_no + dividend_year` |
| 要保 | `insurance_application` | 要保申請 | `application_no` |
| 要保關係人 | `application_party` | 要保關係人 | `application_no + party_role_code + party_seq` |
| 健康告知 | `health_disclosure` | 健康告知 | `application_no + insured_customer_id + question_code` |
| 核保 | `underwriting_case` | 核保案件 | `underwriting_case_no` |
| 核保條件 | `underwriting_condition` | 核保附加條件 | 加費、除外、延期等條件明細 |
| 保全案件 | `policy_change_case` | 保全案件 | `change_case_no` |
| 保全項目 | `policy_change_item` | 保全項目 | `change_case_no + change_item_code` |
| 保全欄位異動 | `policy_change_field` | 保全欄位異動 | 保存異動前後值與完整業務鍵 |
| 保全紀錄快照 | `policy_change_record_snapshot` | 保全紀錄快照 | 保存一筆來源紀錄的異動前後 JSON |
| 保全附件 | `policy_change_attachment` | 保全附件 | `attachment_id`；不使用 `file` 表示資料實體 |
| 理賠案件 | `claim_case` | 理賠案件 | `claim_case_no` |
| 理賠事故 | `claim_event` | 保險事故 | `claim_case_no + event_seq` |
| 理賠給付 | `claim_benefit` | 理賠給付 | `claim_case_no + benefit_item_seq` |
| 理賠文件 | `claim_document` | 理賠文件 | `claim_case_no + document_id` |
| 投資帳戶 | `policy_investment_account` | 投資型保單帳戶 | `policy_no + account_seq` |
| 投資持有 | `policy_fund_holding` | 投資標的持有 | `policy_no + fund_code + valuation_date` |
| 年金給付 | `annuity_payment` | 年金給付 | `policy_no + payment_seq` |
| 代碼定義 | `code_definition` | 代碼定義 | `code_group + code_field + code_before` |
| 覆核主檔 | `change_review` | 異動覆核 | `review_id`／`review_key` |
| 覆核稽核 | `change_review_audit` | 覆核稽核軌跡 | `audit_id`／`event_id` |
| 使用者 | `user_account` | 使用者帳號 | `user_id` |
| 使用者角色 | `user_role_assignment` | 使用者角色指派 | `user_id + role_code` |
| 畫面授權 | `user_screen_authorization` | 使用者畫面授權 | `user_id + function_code` |

### 不建議的資料表命名

| 不建議名稱 | canonical table | 原因 |
| --- | --- | --- |
| `main_policy_master` | `policy_contract` | `main`、`master` 都未增加契約語意 |
| `main_policy_ride` | `policy_coverage` | `ride` 英文錯誤，而且此表同時存主約與附約 |
| `main_policy_address` | `policy_contact` 或拆成 `policy_address`＋`policy_contact` | 現況同時存地址、email、電話、手機 |
| `policy_change_file` | `policy_change_record_snapshot` | 現況保存來源紀錄前後 JSON，不是檔案或附件 |
| `users` | `user_account` | 表名與 `user_id` 身分模型不一致 |
| `authorities` | `user_role_assignment` | authorities 是框架術語，未表達角色指派 |
| `code_description` | `code_definition` | 資料不只有說明，亦包含轉換與狀態 |

## 19. 壽險共通業務邏輯

本節定義跨畫面與跨資料表的業務不變量。法令、商品條款及公司核保／理賠規章優先於本節；費率、年齡、金額與期間上限不得寫死在程式，應由商品版本或規則服務提供。

### 19.1 客戶與關係人

1. `customer_id` 是公司內部穩定識別，不以姓名、身分證號或登入帳號當主鍵。
2. 要保人、被保險人、受益人是「保單角色」，同一客戶可同時具多個角色；不可在客戶主檔增加 `is_applicant` 等永久旗標。
3. 關係人資料須保存保單時點快照，客戶主檔後續修改不得回寫改變歷史契約內容。
4. 受益人同一給付類型與順位的比例合計須符合商品規則；未指定受益人依條款及法令處理，不由畫面自行推定。
5. 身分證件、銀行帳號、健康與醫療資料須標示個資等級，API、Log、匯出與畫面分別執行遮罩及授權。

### 19.2 要保與核保

1. 要保申請、核保案件與正式保單是不同生命週期；核保通過及承保條件完成前不可建立有效契約。
2. 健康告知保存「題目版本＋題號＋答案＋補充明細」，不可只保存最新題目文字。
3. 核保決定至少區分承保、附條件承保、延期與拒保；流程狀態不可取代最終決定。
4. 加費、除外與特別約定應建立明細及有效期間，不以備註文字取代結構化資料。
5. 商品代碼、版本、生效日、保額、保費、繳費期間及保險期間必須使用同一商品版本規則驗證。

### 19.3 保單契約與保障項目

1. `policy_contract` 是契約層級；`policy_coverage` 是主約／附約保障項目，不得把附約欄位塞回契約主檔。
2. 每張保單原則上只有一個 BASE 保障項目；RIDER 必須符合主約商品、被保險人、投保年齡及有效期間規則。
3. 附約有效期間不得超過其依附契約可承保期間；主約終止時須依條款決定附約終止或延續，不可只刪主約列。
4. 金額一律保存幣別與精度；顯示格式不得改變原始金額，彙總值須可由明細重算及對帳。
5. 契約狀態轉換須使用狀態機；不得任意從終止／滿期回到有效。

### 19.4 保費與帳務

1. 應繳、實收、首期、續期、躉繳與總保費是不同概念，不得共用 `premium`。
2. 收款紀錄不可覆寫；沖銷、退款、退票與補收以反向或後續交易保存稽核軌跡。
3. 保費彙總必須以有效保障項目及指定計算基準日重算，並校驗幣別一致。
4. 銀行帳號與信用卡資訊不得存入地址或一般備註；完整卡號交由合規支付服務保存。

### 19.5 保全變更與覆核

1. 每次保全異動須有 `change_case_no`、`change_item_code`、完整業務鍵、異動前後快照、建檔人及時間。
2. 相同 `function_code + unique_key` 同時間只能有一筆處理中資料；檢查與建立必須在同一交易並鎖定。
3. 建檔與覆核必須職務分離；建檔人不可核准自己的案件。Admin 直接完成亦須建立 `S` 稽核軌跡。
4. 正式表只保存最後已生效版本。Maker 提交時只寫 staging／review；覆核為 `S` 才在同一交易套用正式表，`C` 不可改變正式資料。
5. 修改或刪除送審前須比對正式資料版本；覆核套用時再次檢查 `record_version` 或異動前值，避免覆蓋其他已完成異動。
6. `review_key` 是每次提交的唯一技術 ID；防重使用 `function_code + unique_key`，不得用完整 JSON 組成 Key。
7. 稽核事件採 append-only；不得更新或刪除過去事件。每次狀態轉換保存 before/after、operator、request ID、trace ID 及時間。

### 19.6 理賠

1. 理賠案件、保險事故、申請給付項目、核定結果及實際付款分表保存。
2. 理賠資格以事故日當時契約與保障項目有效狀態判斷，不以查詢當日狀態替代。
3. 申請金額、核定金額與實付金額不可共用欄位；部分給付及拒賠須保存原因代碼與說明。
4. 理賠付款不可因重新核定而覆寫；差額給付或追回另建交易並可追溯原付款。

### 19.7 授權與資料治理

1. 角色決定可執行的操作，`user_screen_authorization` 決定可見畫面；畫面授權直接掛 `user_id`，不由角色自動推導保存結果。
2. 前端隱藏按鈕不等於授權；每個新增、修改、刪除、覆核及授權 API 都必須由後端驗證權限。
3. 中文欄位名稱、型態、長度與選項由後端 metadata 提供；前端只負責呈現與基本輸入檢核。
4. 所有日期時間保存明確時區語意；業務日期與系統時間不可混用。
5. 刪除保險業務資料原則上採狀態終止或邏輯刪除；不得破壞外鍵、保全、理賠與稽核歷史。

## 20. 參考依據

- [金融監督管理委員會－保險法](https://law.fsc.gov.tw/LawContent.aspx?id=FL006746)：要保人、被保險人、受益人、保險業務員及人身保險法定用語。
- [金融監督管理委員會－人身保險要保書示範內容及注意事項](https://law.fsc.gov.tw/LawContent.aspx?id=FE060900)：要保人、被保險人、受益人基本資料及要保事項。
- [壽險公會法規系統－團體一年定期人壽保險單示範條款](https://law.lia-roc.org.tw/Law/Content?lsid=FL006852)：保險契約、要保書、被保險人名冊及批註等市場用語。
- [行政院公報－人身保險要保書欄位](https://gazette.nat.gov.tw/EG_FileManager/eguploadpub/eg014231/ch04/type2/gov36/num9/OEg.pdf)：業務員代號／姓名等公開欄位名稱。
- [財政部電子資料交換手冊](https://download.tax.nat.gov.tw/idx/IDX%E4%BE%86%E6%AA%94%E5%96%AE%E4%BD%8D%E6%93%8D%E4%BD%9C%E6%89%8B%E5%86%8A.pdf)：保單號碼、被保險人識別、幣別、給付金額等交換欄位範例。
