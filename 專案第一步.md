# 專案第一步：新增專案時的文件搬移清單

## 目的

建立新的保險專案時，先搬移可跨專案共用的治理文件與 Skills，再依新專案實際技術棧、業務範圍及交付方式刪減。不得直接複製本專案的功能名稱、API、資料表、錯誤碼、Store、畫面、測試資料或部署設定。

文件搬移完成後，新專案必須能回答：

1. AI 在開始工作前要讀哪些規範？
2. 分析、實作、審查及發布的權限邊界是什麼？
3. 資料庫到 UI 的唯一契約如何維持？
4. 哪些規則可共用，哪些必須留在新專案自己的 feature？
5. 如何驗證文件與實際程式一致？
6. Codex 如何在 repository 中自動探索這些 Skills，且如何避免維護兩份副本？

## 搬移前的收斂原則：讓文件可跨專案重用

本文件是「專案治理模板」，不是本 repository 的既有產品快照。搬移到下一個專案時，必須先收斂以下內容：

- 先刪除本專案的具體功能敘述、API path、資料表名、狀態碼、部署 URL、畫面名稱與產品文案；僅保留可重用的治理規則與工作流程。
- 依新專案實際技術棧決定是否保留保險專案、Java/MyBatis、Vue、Batch、介接與對帳相關 Skill；不是實際需要的章節直接省略，不要保留為了「看起來完整」而新增的模組。
- 佔位符一定要明確標示為 `'<feature>'`、`'<resource>'`、`'<backend-module>'` 等，不能以本 repository 的檔名、路徑或業務名稱冒充新專案現況。
- 新專案若不是保險專案，應跳過 `start-insurance-project`、`insurance-requirement-modeler`、`insurance-interface-mapper`、`insurance-reconciliation-analyzer` 等保險領域能力；若不是 Java/MyBatis，則不搬 `enforce-mybatis-three-layer`。
- 如同一份文件中同時存在「保險專案模板」與「一般專案模板」，應以明確條件分段保留，避免未核實的技術要求混在同一份規範中。

## 可搬移性分級

| 分級 | 內容 | 搬移規則 |
|---|---|---|
| 可整組搬移 | 已選定的 `skills/<skill-name>/` | `SKILL.md`、`agents/openai.yaml`、`references/`、`scripts/` 必須整組搬；不得只複製入口文件。 |
| 搬後必須改寫 | `AGENTS.md`、`WORKFLOWS.md`、`.github/copilot-instructions.md` | 依目標專案刪除未採用技術與 Skill，替換 module、package、建置及驗證方式。 |
| 條件式搬移 | `docs/analysis/framework-standards/` | 只有目標技術棧與治理決策適用時搬移；覆蓋率、API 棄用期、錯誤格式等數值仍須重新核准。 |
| 不直接搬移 | 本 repository 的 `README.md`、產品程式、migration、部署設定與功能分析文件 | 只能作來源參考；目標專案必須依自己的現況重建或以明確移植任務處理。 |

「可整組搬移」只代表 Skill 本身沒有來源專案識別，不代表 Skill 所描述的技術一定適用。搬移前仍須依下列層級選擇，搬移後再執行一致性與可搬移性檢查。

## Skill 維護來源與 Codex 自動探索

本模板以 `skills/<skill-name>/` 作為可閱讀、可搬移及可由其他工具引用的唯一維護來源。Codex 的 repository Skill 自動探索位置使用 `.agents/skills/`；需要自動探索時，為每個實際保留的 Skill 建立 symlink：

```text
.agents/skills/<skill-name> -> ../../skills/<skill-name>
```

執行前必須確認相對路徑符合目標 repository 的實際目錄。禁止把整個 Skill 複製到 `.agents/skills/` 後同時維護兩份內容。若目標環境不支援 symlink，必須選定 `skills/` 或 `.agents/skills/` 其中一處作為唯一來源，並同步修改 `AGENTS.md`、README、Workflow 連結及驗證指令。

`.agents/skills/` 只放 Codex 探索入口，不放 `WORKFLOWS.md`；跨 Skill 路由仍由 repository 根目錄的 `WORKFLOWS.md` 管理。

## 第一層：每個新專案都要搬

| 來源 | 新專案位置 | 搬移方式 |
|---|---|---|
| `AGENTS.md` | `AGENTS.md` | 搬移後必須逐段檢查；刪除不存在的模組、畫面、表名、狀態碼及部署平台規則。 |
| `WORKFLOWS.md` | `WORKFLOWS.md` | 搬移 AI 流程入口；依新專案實際保留的 Skills 更新路由表與連結。 |
| `.github/copilot-instructions.md` | `.github/copilot-instructions.md` | 保留語言、證據、實作邊界及驗證原則；依新專案目錄與技術棧調整路徑。 |
| `skills/enforce-code-writing-standards/` | 同路徑 | 整個目錄搬移，包含 `SKILL.md`、`agents/openai.yaml`、`references/` 與 `scripts/`。 |
| `skills/implement-full-stack-feature/` | 同路徑 | 用於使用者明確要求全端實作時。 |
| `skills/fix-full-stack-bug/` | 同路徑 | 用於可重現缺陷的根因分析與修復。 |
| `skills/review-generated-code/` | 同路徑 | 用於唯讀審查 AI 產生的程式與差異。 |
| `skills/release-with-pr/` | 同路徑 | 用於取得明確授權後的 commit、push 與 PR。 |
| `skills/diagnose-deployment/` | 同路徑 | 用於 Docker、資料庫、服務、API 與瀏覽器部署診斷。 |

新專案的 `README.md` 不直接覆蓋。應建立新的 README，說明新專案目的、實際目錄、啟動方式、功能範圍、驗證指令與已存在的 Skills。

## 第二層：Java／MyBatis／Vue 專案要搬

新專案實際採用 Spring Boot、MyBatis、MySQL、Flyway、Vue 3 與 TypeScript 時，搬移：

```text
skills/enforce-mybatis-three-layer/
docs/analysis/framework-standards/
├── 00-development-readiness.md
├── 01-exception-handling.md
├── 02-transaction-concurrency.md
├── 03-api-versioning.md
├── 04-pagination-sorting.md
├── 05-audit-log.md
├── 06-frontend-state.md
└── 07-testing-strategy.md
```

搬移後依新專案確認：

- Java package root、後端與前端目錄名稱。
- API wrapper、錯誤處理及分頁型別是否已存在。
- 使用 MyBatis XML 或其他 persistence 技術。
- 是否已有 Pinia Store；沒有實際跨 route 狀態時，不因文件而建立 Store。
- 是否已有共用 component、style、typed API client 及 OpenAPI 流程。
- 測試、Docker、CI 與 migration 工具是否實際可用。

若新專案不是 MyBatis，不搬 `enforce-mybatis-three-layer`；改寫相關 AGENTS 規則，不得保留互相矛盾的技術要求。

## 第三層：保險專案依需求搬

### 保險新專案開案

搬移：

```text
skills/start-insurance-project/
skills/insurance-requirement-modeler/
skills/spec-generator/
skills/impact-analysis/
skills/test-case-generator/
```

這些 Skill 保留領域建模、證據分級、跨層契約與測試追溯方法，不代表新專案已經具有核保、理賠、保全、收費或要保作業功能。

### 有舊系統需要分析

依序搬移：

```text
skills/legacy-code-explainer/
skills/business-rule-extractor/
skills/spec-generator/
skills/impact-analysis/
skills/test-case-generator/
```

只在實際需要時再搬：

| Skill | 適用情境 |
|---|---|
| `java-code-analysis` | 追蹤 Java 執行路徑與例外根因。 |
| `sql-analyzer` | 分析 SQL、Mapper、schema、lock 與 migration。 |
| `java-refactor` | 已授權修改舊 Java，並需要保持既有行為。 |
| `liberty-debugger` | 新專案或舊系統實際使用 Liberty／WAR／JNDI。 |
| `svn-review` | 原始碼實際由 SVN 管理。 |

### 有外部介接或批次對帳

| Skill | 適用情境 |
|---|---|
| `insurance-interface-mapper` | 固定長度檔、CSV、XML、JSON、MQ、API 或交易夥伴 mapping。 |
| `insurance-reconciliation-analyzer` | 筆數、金額、缺漏、重複、重跑、replay 或 reversal 對帳。 |

### 有明確架構變化點

只有新增抽象、多種規則實作、外部供應商轉接、複雜狀態轉換或批次骨架確有變化點時，搬移：

```text
skills/design-pattern-guide/
```

不得為了「看起來完整」預先建立只有一個實作的 Strategy、Factory、Adapter 或萬用 Service。

## 不要直接搬到新專案的內容

以下內容必須由新專案的正式需求、schema 或現有程式重新確認：

- 本專案 `README.md` 中的實際功能、路由、代碼筆數、商品、階段碼與畫面說明。
- `<backend-module>/`、`<frontend-module>/`、`<batch-module>/` 的產品程式與 migration。
- 特定 API path、Java class、TypeScript type、Vue View、Store 或 component 名稱。
- 特定資料表、欄位、索引、錯誤碼、狀態碼與測試資料。
- Docker port、host、schema、帳號、secret alias、雲端平台及部署 URL。
- 本專案已完成或尚未完成的功能敘述。

需要複用產品程式時，應以明確功能移植任務處理，完成需求、授權、資料契約、相依套件、migration 與測試評估；不得把產品程式當成開案文件直接複製。

## 搬移後必做調整

1. 先列出新專案實際存在的 module、feature、package、API、資料表、前端目錄與建置工具。
2. 搜尋文件中的舊專案名稱、絕對路徑、port、URL、schema、類別、Store、錯誤碼與表名。
3. 能跨功能共用的規則才保留；單一功能業務邏輯移至該 feature 的需求或分析文件。
4. 不存在但需要表達格式的位置，改用 `<feature>`、`<resource>`、`<Feature>` 等明確佔位符。
5. 更新新專案 README 的文件清單、Skill 清單與驗證方式。
6. 每個 Skill 必須保留 `SKILL.md` 與 `agents/openai.yaml`；若有 `references/` 或 `scripts/`，整組搬移，不可只複製入口文件。
7. 執行 Skill 驗證、文件掃描及連結檢查。
8. Java 分層掃描必須傳入目標 repository 實際的 `<java-package-root>`；檢查程式沒有預設 package，也不得沿用來源專案 package。
9. 為需要 Codex 自動探索的 Skills 建立 `.agents/skills/` symlink，逐一確認 target 存在且仍指向唯一維護來源。
10. 每個 Skill 至少以正向觸發、不得觸發及與鄰近 Skill 重疊三類提示驗證；description 必須能讓 Agent 選到正確 Skill，不能只通過 Markdown 格式檢查。
11. 確認每個 `references/` 文件都由所屬 `SKILL.md` 直接連結並標示讀取時機，不保留孤立 reference 或深層轉引。
12. 依 `WORKFLOWS.md` 逐項確認單一入口、契約閘門、證據閘門、有限修正、失敗封閉及交接規則仍存在。

```bash
python3 skills/enforce-code-writing-standards/scripts/check_workflow_consistency.py .
python3 skills/enforce-code-writing-standards/scripts/check_skill_portability.py .
python3 -m unittest skills/enforce-code-writing-standards/scripts/test_check_workflow_consistency.py
python3 -m unittest skills/enforce-code-writing-standards/scripts/test_check_skill_portability.py
```

## 建議的新專案最小目錄

```text
new-project/
├── AGENTS.md
├── README.md
├── WORKFLOWS.md
├── 專案第一步.md
├── .agents/
│   └── skills/
│       ├── enforce-code-writing-standards -> ../../skills/enforce-code-writing-standards
│       └── <skill-name> -> ../../skills/<skill-name>
├── .github/
│   └── copilot-instructions.md
├── docs/
│   └── analysis/
│       └── framework-standards/    # 技術棧適用時才建立
└── skills/
    ├── enforce-code-writing-standards/
    ├── implement-full-stack-feature/
    ├── fix-full-stack-bug/
    ├── review-generated-code/
    ├── release-with-pr/
    └── diagnose-deployment/
```

保險領域、MyBatis、舊系統分析、介接、對帳及設計模式 Skills 依前述條件加入，不要求每個新專案全部搬入。

## 完成檢查表

- [ ] `AGENTS.md` 已改成新專案實際規範。
- [ ] `WORKFLOWS.md` 只連結新專案實際保留的 Skills。
- [ ] `WORKFLOWS.md` 保留單一入口、契約閘門、證據閘門、有限修正、失敗封閉及交接模式。
- [ ] `WORKFLOWS.md` 的禁止項目已依新專案調整，且沒有刪除「禁止假裝讀過規範」與「禁止虛假驗證」。
- [ ] `.github/copilot-instructions.md` 與 `AGENTS.md` 沒有矛盾。
- [ ] README 已描述新專案，不是舊 README 的副本。
- [ ] 文件沒有不存在的 Store、類別、API、表名、錯誤碼或畫面。
- [ ] 共用規則至少有兩個相同責任的實際使用者，或屬全專案技術契約。
- [ ] 單一功能業務規則留在該 feature，不放入通用 workflow。
- [ ] 每個已搬 Skill 的 `SKILL.md`、`agents/openai.yaml` 及必要資源完整。
- [ ] 每個 Skill description 已驗證正向、不觸發及重疊情境，不會因相似關鍵字誤選主要流程。
- [ ] 每個 reference 都由所屬 `SKILL.md` 直接連結，沒有孤立或必須深層追蹤的規範。
- [ ] `.agents/skills/` 只包含指向唯一 Skill 來源的有效 symlink；沒有第二份可編輯副本。
- [ ] 文件內連結與指令可在新專案重現。
- [ ] Skill validation 與文件掃描通過。
- [ ] 未驗證項目已明確列出，沒有以假設描述為現況。
